*.txt files dropped here will be processed by config/buildauto.py to create config/autoplaylist.txt. Basically, include line-separated lists of youtube links.
Files in here can be loaded as individual playlists to sub in for autoplaylist.txt by using the Discord command "apl".